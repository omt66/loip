import { displaySystemInfo, displayIPAddresses,  } from "./utils/index"

// Let's display the system and IP information when the script runs!
function main() {
    displaySystemInfo()
    displayIPAddresses()
}

main()
